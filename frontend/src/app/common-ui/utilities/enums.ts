export enum Theme {
    LIGHT = 0,
    DARK = 1
}

export enum UserRole {
    ADMIN = 0,
    MODTR = 1,
    PRO = 2,
    USER = 3
}

export enum DisplayType {
    XS = 0,
    SM = 1,
    MD = 2,
    LG = 3,
    XL = 4
}

export enum LogInState {
    LOGGEDIN = 0,
    LOGGEDOUT = 1
}